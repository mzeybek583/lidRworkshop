library(lidR)
rm(list = ls(globalenv()))

# ======================================
#         AREA BASED APPROACH
# ======================================

ctg = catalog("data/Farm_A/")
col = height.colors(50)

# A. Exercise
# =================

# Compute some metrics of interest on the dataset.
# According to a dummy relashionship such as B = 2*max(Z) + 1.2*mean(Z) - 3*sd(Z) try to map B with
# a resolution of 100 m²



